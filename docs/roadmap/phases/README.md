# Roadmap Phases

These files are historical phase briefs and implementation intent snapshots.

They are not canonical runtime truth.
For current behavior, use:
- `docs/index.md`
- `docs/architecture/*`
- `docs/presentation/*`
- implementation files under `services/*` and `database/*`